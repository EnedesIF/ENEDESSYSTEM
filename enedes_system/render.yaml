sservices:
  - type: web
    name: enedes-2
    env: docker
    plan: free
    autoDeploy: true
    buildCommand: ""
    # CORRIGIDO: Removido startCommand incorreto para Apache
    # Nginx roda automaticamente via Dockerfile CMD
    dockerfilePath: ./Dockerfile
